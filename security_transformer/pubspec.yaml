name: security_transformer
description: Security transformer

environment:
  sdk: '>=1.20.1 <=2.0.0-dev.8.0'

dependencies:
  secdart:
      path: ../secdart
  secdart_analyzer:
    path: ../sec_analyzer
  analyzer:
  barback: ">=0.14.1 <0.16.0"
  dart_style:

dev_dependencies:
  test_reflective_loader: "^0.1.3"
  test: ^0.12.0

transformers:
  - security_transformer